{
  "name": "@dni-playground/server",
  "version": "1.0.0",
  "description": "Backend",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "rimraf dist && webpack --progress",
    "build:watch": "rimraf dist && webpack --watch",
    "dev": "concurrently --kill-others \"rimraf dist && webpack --watch\" \"nodemon dist/main.js\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^3.1.5",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "nodemon": "^2.0.21"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.3",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/express-session": "^1.17.6"
  }
}
